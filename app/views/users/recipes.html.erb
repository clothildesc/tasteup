<div class="mt-4 pt-1 bg-white rounded-2">
  <div class="container">
    <div>
      <% if @user == current_user %>
        <h3 class="title">Mon carnet de recettes 🧑‍🍳</h3>
      <% else %>
        <div class="d-flex align-items-center">
        <%= image_tag @user.photo, class: "avatar" %>
        <h3 class="title ms-2">Les recettes de <%= @user.first_name %> 🧑‍🍳</h3>
      </div>
        <% if current_user.favorited?(@user) %>
          <%= button_to 'Suivi(e)', follow_user_path(@user), data: { turbo_method: :post } %>
        <% else %>
          <%= button_to "Suivre", follow_user_path(@user), data: { turbo_method: :post } %>
        <% end %>
      <% end %>
      <div class= "d-flex overflow-x-auto p-2">
        <div class="input-grid">
          <%= render "recipes/card", recipes: @recipes %>
        </div>
      </div>
    </div>
  </div>
</div>
