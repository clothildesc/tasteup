<div class="mt-4 pt-1 rounded-2">
  <div class="container">
    <div>
      <% if @user == current_user %>
        <h3 class="title text-center">Mon carnet de recettes 🧑‍🍳</h3>
      <% else %>
        <div class="profile-header d-flex align-items-center gap-2">
          <%= image_tag @user.photo, class: "avatar-large" %>
          <div>
            <h3 class="title">Recettes <%= @user.first_name %> 🧑‍🍳</h3>
            <%= link_to current_user.favorited?(@user) ? "Suivi(e)" : "Suivre", follow_user_path(@user), class: "", data: { turbo_method: :post } %>
          </div>
        </div>
      <% end %>
      <div class="container my-3">
        <%= render "recipes/search_bar", path: recipes_user_path(@user)%>
      </div>
      <div class= "d-flex overflow-x-auto p-2">
        <div class="input-grid">
          <%= render "recipes/card", recipes: @recipes %>
        </div>
      </div>
    </div>
  </div>
</div>
