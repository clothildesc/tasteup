<div class="mt-4 pt-1 bg-white rounded-2">
  <div class="container">
    <div class="d-flex justify-content-between align-items-center">
      <% if @user == current_user %>
        <h3 class="title">Toutes mes pépites 🤩</h3>
      <% else %>
        <h3 class="title">Toutes les pépites de <%= @user.email %> 🤩</h3>
      <% end %>
    </div>
    <%= render "recipes/recipes", recipes: @recipes %>
  </div>
</div>
